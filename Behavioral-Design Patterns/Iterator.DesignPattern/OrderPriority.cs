/// <summary>
/// Domain-specific priority levels for order processing.
/// Higher values indicate more critical orders.
/// </summary>
public enum OrderPriority
{
    Low = 1,
    Standard = 2,
    High = 3,
    Critical = 4
}
